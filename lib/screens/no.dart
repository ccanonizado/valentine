import 'package:flutter/material.dart';

class No extends StatefulWidget {
  No({Key key}) : super(key: key);

  @override
  _HomeState createState() => _HomeState();
}

class _HomeState extends State<No> {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('No'),
    );
  }
}
